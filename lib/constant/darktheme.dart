import 'package:flutter/material.dart';

//appbar
const Color appBarBackgroundColor = Color(0xFF1E1E1E); //background de appbar
const Color appBarTextIconColor = Color(0xFFEAEAEA); //icono y text

//body
const Color backgroundColor =
    Color.fromARGB(255, 43, 42, 42); // Main background
const Color primaryTextColor = Color(0xFFB3B3B3); // Primary text color
const Color redHighlightColor =
    Color(0xFFFF3B3F); // Red highlights (buttons, icons)

// Navigation Bar
const Color navBarBackgroundColor =
    Color(0xFF1E1E1E); // Fondo del Navigation Bar
const Color navBarActiveIconColor = Color(0xFFFF3B3F); // Iconos activos
const Color navBarUnselectedItemColor =
    Color(0xFF7D7D7D); // Íconos y texto no seleccionados

// Card Colors
const Color cardBackgroundColor = Color(0xFF2E2E2E); // Background of the card
const Color cardShadowColor =
    Color.fromARGB(50, 0, 0, 0); // Subtle shadow for depth
const Color cardBorderColor =
    Color(0xFF393939); // Border for a slightly raised look
const Color cardTextColor = Color(0xFFEAEAEA); // Text color for readability
const Color cardHighlightColor =
    Color(0xFFFF3B3F); // Highlight for important elements
const Color cardPriceTextColor = Color(0xFFFF3B3F); // Matches redHighlightColor


